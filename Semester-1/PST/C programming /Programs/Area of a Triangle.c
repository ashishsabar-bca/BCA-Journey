#include <stdio.h>

int main()
{ 
 float pi = 3.14,r,area = 1;
 printf("Enter the radius of the Triangle:");
 scanf("%f",&r);
 area = pi*r*r;
 printf("The area of the Triangle :%f",area);
 return 0;  
}
